import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import TextArea from './TextArea'
import { Form } from 'informed'

<Meta title='Components|UI Kit/TextArea' component={TextArea} />

# TextArea

Custom [informed](https://joepuzzo.github.io/informed/?path=/story/inputs--intro) input.

<Preview>
  <Story name='Default'>
    <Form>
      <TextArea
        field='name'
        label='Write a longer message'
        placeholder='Write a story'
      />
    </Form>
  </Story>
</Preview>

<Props of={TextArea} />
